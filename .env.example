# Required before deploying: your real domain, used for canonical URLs, Open Graph
# tags, robots.txt, and the sitemap. Without this set, the site falls back to a
# clearly-labeled placeholder ("your-domain-here.com") — not a real address.
# Example: https://adityaverma.com
NEXT_PUBLIC_SITE_URL=
